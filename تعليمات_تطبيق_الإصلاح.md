# تعليمات تطبيق إصلاح مشكلة "غير مصرح" عند إنهاء الصيانة

**ملاحظة:** مسؤول الصيانة (maintenance_manager) يمكنه الآن إنهاء الصيانة وحفظها.

## الخطوة 1: فتح لوحة تحكم Supabase

1. ادخل إلى موقع [supabase.com](https://supabase.com)
2. سجّل الدخول بحسابك
3. اختر مشروعك (Project)

---

## الخطوة 2: فتح محرر SQL

1. من القائمة الجانبية اضغط على **SQL Editor**
2. اضغط **New query** لإنشاء استعلام جديد

---

## الخطوة 3: نسخ ولصق الكود الأول

انسخ الكود التالي بالكامل والصقه في المحرر، ثم اضغط **Run** (أو Ctrl+Enter):

```sql
-- إصلاح 1: التأكد من وجود جميع المستخدمين في user_profiles
INSERT INTO public.user_profiles (id, full_name, role, created_at)
SELECT u.id, COALESCE(p.full_name, ''), COALESCE(u.raw_app_meta_data->>'user_role', 'driver'), now()
FROM auth.users u
LEFT JOIN public.user_profiles p ON p.id = u.id
WHERE p.id IS NULL;

-- إصلاح 2: مزامنة الدور في JWT
UPDATE auth.users u
   SET raw_app_meta_data =
       COALESCE(u.raw_app_meta_data, '{}'::jsonb)
       || jsonb_build_object('user_role', p.role)
  FROM public.user_profiles p
 WHERE u.id = p.id
   AND (u.raw_app_meta_data ->> 'user_role') IS DISTINCT FROM p.role;
```

انتظر حتى تظهر رسالة **Success** في الأسفل.

---

## الخطوة 4: تعيين الأدوار للمستخدمين

**لحساب الأدمن:** غيّر البريد و `admin` في السطر الأخير:
**لحساب مسؤول الصيانة:** غيّر البريد و `maintenance_manager` في السطر الأخير:

```sql
DO $$
DECLARE
  v_user_id UUID;
  v_email TEXT := 'your-email@example.com';  -- <<<< ضع البريد هنا
  v_role TEXT := 'admin';  -- <<<< ضع 'admin' أو 'maintenance_manager'
BEGIN
  SELECT id INTO v_user_id FROM auth.users WHERE email = v_email;
  IF v_user_id IS NOT NULL THEN
    INSERT INTO public.user_profiles (id, full_name, role, created_at)
    VALUES (v_user_id, COALESCE((SELECT full_name FROM public.user_profiles WHERE id = v_user_id), ''), v_role, now())
    ON CONFLICT (id) DO UPDATE SET role = v_role;
    
    UPDATE auth.users
    SET raw_app_meta_data = COALESCE(raw_app_meta_data, '{}'::jsonb) || jsonb_build_object('user_role', v_role)
    WHERE id = v_user_id;
    
    RAISE NOTICE 'تم تعيين الدور % بنجاح لـ %', v_role, v_email;
  ELSE
    RAISE NOTICE 'المستخدم غير موجود: %', v_email;
  END IF;
END $$;
```

**مثال لمسؤول الصيانة (ثائر):**
- `v_email := 'thaer@alhasani.com'`
- `v_role := 'maintenance_manager'`

---

## الخطوة 5: تشغيل السكربت الرئيسي

1. افتح ملف: `supabase/migrations/20260310080000_fix_unauthorized_and_images.sql`
2. انسخ **كل** محتوى الملف
3. الصقه في SQL Editor في Supabase
4. اضغط **Run**

انتظر حتى يظهر **Success** بدون أخطاء.

---

## الخطوة 6: تسجيل الخروج والدخول من جديد

1. ارجع إلى موقعك (التطبيق)
2. اضغط **تسجيل الخروج**
3. سجّل الدخول مرة أخرى بنفس البريد

---

## الخطوة 7: التجربة

1. ادخل إلى **الصيانة النشطة**
2. اضغط **إنهاء الصيانة**
3. املأ الحقول واضغط **إنهاء وحفظ**

إذا ظهرت أي رسالة خطأ، انسخها وأرسلها.

---

## التحقق من الدور يدوياً (اختياري)

إذا استمرت المشكلة:

1. في Supabase اذهب إلى **Table Editor**
2. اختر جدول **user_profiles**
3. ابحث عن صفك (بريدك أو اسمك)
4. تأكد أن عمود **role** يحتوي على القيمة `admin` بالضبط (حروف صغيرة، بدون مسافات)

---

## إذا لم ينجح شيء

تأكد من:
- أنك تستخدم البريد الإلكتروني الصحيح في الخطوة 4
- أنك سجّلت الخروج والدخول بعد تنفيذ الخطوات
- أن المتصفح لا يستخدم كاش قديم (جرّب Ctrl+Shift+R أو نافذة خاصة)
